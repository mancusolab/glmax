# Release Notes

This page records release-facing notes for user-visible contract changes.

## Unreleased

- Breaking change: `InferenceResult.z` has been renamed to `InferenceResult.stat`.
  Update downstream attribute access accordingly.
